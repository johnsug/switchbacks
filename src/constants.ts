/**
 * App-wide constants.
 *
 * STRAVA_RELAY_URL: after deploying the Cloudflare worker (`worker/`),
 * replace the placeholder below with your worker's URL.
 * Everyone who clones the repo will automatically use the same relay.
 */
export const STRAVA_RELAY_URL = "https://switchbacks-relay.johnsug.workers.dev";
